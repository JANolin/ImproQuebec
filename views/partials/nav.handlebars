<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <a class="navbar-brand" href="/">ImproQuebec</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav mr-auto">
                <li class="nav-item">
                    <a class="nav-link" href="/accueil">Accueil</a>
                </li>
                {{#if user_role.equipe}}
                <li class="nav-item">
                    <a class="nav-link" href="/match">Match</a>
                </li>
                {{/if}}
                {{#if user_role.coach}}
                <li class="nav-item">
                    <a class="nav-link" href="/validation">Validation des feuilles de match</a>
                </li>
                {{/if}}
                {{#if user_role.admin}}
                <li class="nav-item">
                    <a class="nav-link" href="/ajoutequipe">Ajout equipe</a>
                </li>
                {{/if}}
                <li class="nav-item">
                    <a class="nav-link" href="/historique">Historique</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/equipes">Equipes</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/horaire">Horaire</a>
                </li>
            </ul>
            {{#if user_context}}
            <form class="form-inline my-2 my-lg-0">
                <a class="btn btn-danger" href="/logout" role="button">Se déconnecter</a>
            </form>
            {{/if}}
            {{#if user_role.guest}}
            <form class="form-inline my-2 my-lg-0">
                <a class="btn btn-normal" href="/login" role="button">Se connecter</a>
            </form>
            {{/if}}
    </div>
</nav>
